# PR 意图声明 — feat/lizi-upstream-port

## 解决什么问题

review-pr 的 signoff 维护者确认门从上游移植后存在三处缺陷：

1. **持久放行粒度过粗**：上游历史 Approve 不带门类（kind），对 security 门的一次 Approve 会连带放行之后新出现的 rules 门——等于一次授权永久覆盖所有未来的门，把门变成摆设。
2. **放行自动关 issue 无来源校验**：上游 close-issue 不验证放行标记作者，任何能贴 marker 的人都能关掉维护者开的讨论 issue（伪 marker 关错 issue）。
3. **翻案保护与 dry-run 不一致**：dry-run 预演对已 resolve 又 reopen（人工翻案）的线程仍预演会回复，与真实执行 0 reply 不符，预演结果不可信；同 headSha 时 wouldReply 无条件 true 同样偏离实执。

## 为谁解决

review-pr 的维护者（admins 名单成员）与使用 signoff 门的仓库 owner：
- 维护者不再需要重复确认同一门类，但新门类不会被旧授权连带放行（安全边界不缩水）；
- 讨论 issue 只会在受信任作者的放行标记下被决策关闭，不会被伪 marker 关闭。

## 成功长什么样

- 一次 admin Approve（当前 head）确认该 head 上全部已触发门类；后续 head 仅当触发**新的**门类时才重新走确认门——已确认门类继续放行（由放行标记携带，作者校验后收集）。
- 决策层输出 `closeOnRelease`：仅 released=true 且存在受信任作者 hold marker 时建议关闭，伪 marker 被列出并拒绝。
- dry-run 预演与真实执行的 reply 语义完全一致（760/760 测试全绿证明，含反向变异）。
- 产物（dist/、preview-dist/）由合并后源码重建，freshness 全绿。

## 边界（本 PR 不做）

- 不接线关 issue 的**执行**动作：`performIssueClose` 仅落定义，生产调用点随 `signoff-release.mjs` 另立 PR。
- 不改 thread 级年龄门（MIN_MARKER_AGE_MS 10 分钟人工反对窗口），PR 级放行与其正交。
