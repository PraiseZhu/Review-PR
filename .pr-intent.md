# PR 意图声明 — p2-guard-scope

## 解决什么问题

2026-08-10 事故：`signoff-policy.test.mjs` 以自断言脚本形态放在 `scripts/`，不在 `node --test tests/*.test.mjs` 的 glob 覆盖内——四个函数（`isUiTestPath` / `decideIssueReuse` / `shouldCloseDiscussionIssue` / `classifyGateHits`）的唯一测试从标准跑法漏掉，七轮审查与四份终审都没发现，最后从一条测试计数差里掉出来。

文件位置守卫的旧实现有两个洞：
1. **扫描根只到 `review-pr/`**：`agent-use/`、`generate-update-notice/` 等仓根目录罩不到。
2. **`tests/` 豁免过宽**：标准跑法 `node --test tests/*.test.mjs` 是 shell glob、**不递归子目录**，所以 `tests/fixtures/x.test.mjs` 既躲过 glob 又被守卫放行——正是守卫要防的裸奔形态。

## 为谁

- **所有给 review-pr 写测试的 agent**：位置放错的测试文件现在全仓转红并打印违规路径，不可能再无声裸奔。
- **巡审/审查席**：测试计数差类漏检不会再出现（守卫对嵌套子目录也生效）。

## 成功长什么样

- 守卫扫描根 = 仓根（`import.meta.url` 解析，不依赖 cwd）。
- 豁免收窄为：仓根与 `review-pr/` 下的 `dist/`、`preview-dist/` 构建产物镜像 + `review-pr/tests/` 直接子文件；`review-pr/tests/` 嵌套子目录不再豁免。
- 守卫单测 1/1 绿；变异实测：仓根造违规 `*.test.mjs` → 转红并打印路径 → 删除恢复绿。
- 纯测试改动，生产代码零变化，非功能性 PR 免三审。
