# PR 意图声明 — remove-fix-handoff-recipe

## 解决什么问题

#35 只把 5.4 改成「已停用」，正文里仍留着 create/jump 步骤、投递模板、复发升级阶梯，`context.mjs` 的 `auto.note` 仍教 selfFix 开跟进会话，`fix-session-state.mjs` get 在无绑定时还会 `shouldDispatch=true`。agent 照着跑仍会自己开 handoff。

## 为谁

- **巡审 / auto 班车**：卡点只进汇总，不会再开跟进修复会话。
- **selfFixAuthors 作者的 PR**：不再被自动丢进独立 session 改代码。

## 成功长什么样

- SKILL 5.4 没有投递步骤、没有「你负责跟进修复」模板、没有 `use_worktree: true` 操作指令。
- `fix-session-state.mjs get` 的 `shouldDispatch` 恒 false；`set` 拒绝写盘。
- `context.mjs` 的 `auto.note` 不再写「开跟进会话自己修」。
- dist / preview-dist 与源一致；相关测试绿。
