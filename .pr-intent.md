# PR 意图声明 — managed-worktrees-in-repo-dot-worktrees

## 解决什么问题

审查跟进会话的工作树已改落到主仓 `.worktrees/review-pr/`（以及品牌迁移残留的 `.xdt-worktrees`）。回收脚本 `fix-worktree-cleanup.mjs` 原先只认 `.cindy-worktrees` 与 `.claude/worktrees`，会把新落点当成用户自建而永不回收，目录随 PR 线性膨胀。

## 为谁

- **巡审 / 跟进会话维护者**：PR 合并后托管工作树会被清掉，不再占盘。
- **本机各工程仓**：`.worktrees/review-pr/` 是规范落点，回收器必须认它。

## 成功长什么样

- `isManagedPath` 对路径段含 `.cindy-worktrees`、`.xdt-worktrees`、`.claude/worktrees`、`.worktrees/review-pr` 的树返回 true。
- 安全边界不变：仍要 `gh` 实查对应 PR 全部非 OPEN 才动；不 `--force`、不删默认分支；查不到 PR 的不动。
- SKILL.md 托管路径列表与脚本一致。
