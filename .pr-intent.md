# PR 意图声明 — upstream-alignment 对齐文档（p0-lizi-align）

## 解决什么问题

review-pr 对上游 makecindy/cindy-lizi-skills 的七个转人工提交缺少逐条对齐结论，后续 p1 移植 PR 没有决策依据：「照抄上游」是否可行、哪些必须改良、接线落在哪，只能每次重新辩论。

## 为谁

- **p1 移植 PR 的执行席与审查席**：落地形态、改良点、接线位置均在此核定，移植时直接消费结论。
- **review-pr 维护者**：审上游新提交时有一份带两席核定事实的对照基线。

## 成功长什么样

- `review-pr/references/upstream-alignment.md` 存在且覆盖七个上游 sha（`5467af8` `da33085` `fc622f5` `6af6834` `16be74a` `69ab127` `7820392`），每项含「移植 / 不移植 / 合并」三选一结论 + 理由。
- 无「待定 / TBD / TODO」残留。
- 纯文档，零运行时行为改动。
