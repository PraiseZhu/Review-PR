# PR 意图声明 — p1-preview-rootfix

## 解决什么问题

preview 门长期**真红**且无人重建。preview 产物有意含台账副本（freshnessIgnore 为空），巡审每次 append 台账 → 产物真过期 → 门红；重建没有自动路径，只能靠人发现。PR #17 只修了 dist 门的**假红**，preview 门的真红语义被保留下来却无人根治。

## 为谁

- **三审第③席（验收席）**：preview 受限分发版是他们的审查对象，产物过期 = 审的是旧快照。
- **巡审维护者**：不再需要手工发现并重建 preview 产物。
- **所有跑产物门的 agent**：preview 门从「长期红、靠人救」变为「台账写入后自动恢复绿」。

## 成功长什么样

- `evolution-note.mjs`（台账唯一写入口）写盘成功后**自动联动重建 preview-dist**，preview 门自动恢复绿。
- 从 preview 副本运行本脚本时**不会自毁**（outDir 与 SKILL_ROOT 重合即跳过），不再依赖 manifest exclude 配置巧合。
- 22/22 产物门测试绿；去掉联动或去掉自毁守卫时对应测试必须红。
- 已知缺口如实声明：`timeout: 60_000` 无专项测试（去掉它定向测试仍 2/2 绿）。
