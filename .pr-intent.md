# PR 意图声明 — lock-heartbeat-no-idle-refresh

## 解决什么问题

Mini 上 `[Schedule] Mivo review-pr 自动巡审` 把 `refresh-lock.mjs` 当成等待循环打了 4660 次 LLM 回合，cache_read 24.5 亿 token，单轮 $515。根因不是锁文件写贵，是「等子 agent 时用 refresh-lock 空转」被模型当心跳协议执行。只改说明书不够——上次它已经无视过「每个 PR 续一次」。

## 为谁

- **巡审维护者 / owner**：每小时 auto 不再能用续锁空转把同一段对话计费几千遍。
- **后续整点巡审**：锁不会被永续心跳永久占死；一轮结束后所有后台进程必然停。

## 成功长什么样

- 续期由 `lock-heartbeat-daemon.mjs` 后台做（不经 LLM）；主会话禁止拿 `refresh-lock` 当等待。
- 10 分钟内重复调用 `refresh-lock.mjs` 返回 `skipped=cooldown`，不改锁。
- 守护三重自杀：锁文件消失（绝不重建）/ token 换人 / 活满 3 小时。cleanup/release 也会 SIGTERM。
- `tests/session-lock.test.mjs` 钉死冷却、错 token、CLI、守护起停与三条自终止；相关守卫（含 dist/preview freshness）全绿。
